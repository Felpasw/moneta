import { AccountsScreen } from "@/components/templates/AccountsScreen";

export const metadata = {
  title: "Accounts · Moneta",
};

export default function AccountsPage() {
  return <AccountsScreen />;
}
