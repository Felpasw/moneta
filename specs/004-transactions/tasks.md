# Transações, contas, transferências e faturas — backend (MNT-122..140, MNT-146..148, MNT-154..157)

> UI (MNT-141..145: páginas `/transactions`, `/banks`, dashboard KPI, `/invoices/:id`, histórico de faturas) migrada pra `specs/009-ui-shell/tasks.md`.

## Decisões (inline)

- **Contas**: `user_bank_accounts` sem `account_type` — a mesma conta serve pra spending/saving/investing. Cartão de crédito é identificado por `credit_limit` presente + campos de ciclo (`close_day`, `due_day`)
- **Transfers**: tabela própria `transfers` — nunca infla totais de gasto/renda. Reports filtram fora
- **Categorias**: livre-forma por user. **Sem tags**. Seed inicial de categorias default (Alimentação, Transporte, Moradia, Lazer, Saúde, Educação, Assinaturas, Salário, Outros). User pode criar próprias
- **Balance**: `set_balance({ accountId, amount })` sobrescreve o campo. Sistema começa a rastrear a partir daí — sem transação fake de ajuste
- **Cartão de crédito**: modelagem completa de ciclos (Opção B). `credit_card_invoices` amarrada a `user_bank_accounts` com `close_day`/`due_day`. Transações no cartão resolvem `invoice_id` automaticamente pela data. Pagamento detectado via `transfer` com destino cartão + valor match
- **Atomicidade de saldo**: toda operação que altera saldo (transaction insert/delete/edit, transfer, invoice payment) roda numa transação DB única — nunca fica saldo dessincronizado
- **Todo tool novo tem `playbook`** (contrato de MNT-52 pós-atualização). Sem playbook, tool não passa no linter

## Depende de

| Item | Spec | Necessário pra |
|------|------|----------------|
| Auth Fase 1 | 002-auth | Ownership de tudo (userId da sessão) |
| ToolRegistry + Dispatcher | 003-assistant Fase 2 | Registrar as ~15 tools novas |
| `get_tool_help` (MNT-94) | 003-assistant Fase 2.5 | Playbooks on-demand |
| Postgres + Prisma | 002-auth Fase 0 | Migrations |
| UI shell (MNT-98..99) + shadcn foundation (MNT-71) | 009-ui-shell | Onde montar `/transactions`, `/banks`, `/invoices` |

## Convenções

Mesmas do resto dos specs. Bundle onde faz sentido (várias tools do mesmo domínio num commit só).

---

## Fase 0 — Schema base

- [x] **MNT-122** [T][S] Migration `financial_core`: cria em uma leva `banks` (catálogo global), `user_bank_accounts` (com `close_day`, `due_day`, `credit_limit`, `overdraft_limit`, `nickname NOT NULL`), `categories` (user_id nullable — NULL = default global), `transactions` (com `invoice_id` FK opcional pra amarrar cartão), `transfers`, `credit_card_invoices`. FKs, checks e índices conforme modelo aprovado (ver `specs/000-product-brief/spec.md` — modelo financeiro) ✅ commit `db9652c` (schema only; migration SQL + CHECK constraints ficam pra rodar em ambiente com DB)
- [x] **MNT-123** [T][S] Seed: (a) top ~20 bancos BR com nome, código COMPE, logo_url; (b) categorias default globais (Alimentação, Transporte, Moradia, Lazer, Saúde, Educação, Assinaturas, Salário, Investimentos, Outros). Seed roda idempotente (INSERT ... ON CONFLICT DO NOTHING) ✅ commit `db9652c` (logoUrl null nos bancos — assets pendentes)

---

## Fase 1 — Bancos & contas

- [x] **MNT-124** [T][S] Módulo `banks/` (Clean Arch): use-cases `ListBanks` (catálogo público), repository read-only sobre a tabela seedada. Endpoint `GET /banks` ✅ commit `2df01a4` (tool `list_banks` fica em MNT-126)
- [x] **MNT-125** [T][S] Módulo `accounts/`: `UserBankAccountRepository` + use-cases `ListMyAccounts`, `AddBankAccount({ bankId, nickname, initialBalance?, creditLimit?, closeDay?, dueDay? })`, `UpdateBankAccount`, `DeleteBankAccount`, `SetBalance({ accountId, amount })`. Endpoints REST equivalentes. Ownership: filtro `user_id` obrigatório em todos ✅ commit `c9d37dd` (regra "delete só se sem transactions" fica delegada ao FK Restrict do Postgres por ora; validação de UpdateBankAccount ↔ credit card fields fica pra quando o módulo credit-cards existir; tools em MNT-126)
- [x] **MNT-126** [T][S] Tools do assistente (bundle) — cada um com playbook inline: ✅ commit `5940181` (6 tools registradas via @RegisterAssistantTool em sub-módulos <domain>-tools.module.ts dedicados; validação de input via zod dentro do execute; erros de domínio viram ok:false)
  - `list_banks` — lista o catálogo (só read)
  - `list_my_accounts` — lista as contas do user com saldo e limite
  - `add_bank_account({ bankId, nickname, initialBalance?, creditLimit?, closeDay?, dueDay? })` — playbook enfatiza: confirme com user antes; se `creditLimit` presente, `closeDay` e `dueDay` obrigatórios
  - `update_bank_account({ id, ...fields })`
  - `delete_bank_account({ id })` — playbook: só permitir se conta sem transactions; caso contrário orientar o user a arquivar
  - `set_balance({ accountId, amount })` — playbook: confirma valor; explica que sobrescreve balance sem criar transação
- [x] **MNT-158** [T][S] ✅ commit `32f8841` — Enriquecer `GET /accounts` com `bank` embed. Port `UserBankAccount` mantido enxuto; novo tipo `UserBankAccountWithBank extends UserBankAccount { bank: Bank }` só na assinatura de `listByUserId` (outros métodos — `findById`, `add`, `update`, `setBalance` — continuam sem bank, sem quebra cross-módulo). Adapter Prisma: `ACCOUNT_WITH_BANK_SELECT` estende `ACCOUNT_SELECT` com `bank: { select: { id, name, compeCode, logoUrl } }`; `toDomainWithBank` faz só conversão Decimal→number + passa bank nested. `ListMyAccountsUseCase` retorna `UserBankAccountWithBank[]`. Zero migration. Testes novos: `prisma-user-bank-accounts.repository.spec.ts` (2 casos — checking + credit com logoUrl). Fixture do `list-my-accounts.use-case.spec` atualizado com `bank`. Tool `list_my_accounts` recebe o shape enriquecido automaticamente via use-case. Suite completa: baseline 6 suites/16 tests falhando (pré-existentes em `agent/`) → após MNT-158 5 suites/15 falhas. Zero regressão.
- [x] **MNT-159** [T][S] Enriquecer `GET /accounts` com `currentInvoice: { totalAmount, status, dueDate, cycleStart, cycleEnd } | null` + `usagePct: number` pra rows com `creditLimit !== null`. Port `UserBankAccountWithBank` estende com os dois campos (import cross-módulo do `InvoiceStatus` de `card-billing`). Prisma repo faz o join via `select: { invoices: { where: { status: 'open' }, take: 1, orderBy: { cycleStart: 'desc' }, select: {...} } }` — um único `findMany`, zero loop. `toDomainWithBank` mapeia a primeira invoice open (ou null) e computa `usagePct` inline no mesmo pass da conversão Decimal→number: `Math.round(min(totalAmount/creditLimit, 1) * 100)` (checking/sem invoice/creditLimit<=0 → 0; overflow → capped em 100; rounding no int mais próximo). **Divergência do spec:** não usei `CreditCardCycleService.resolveInvoiceForDate` porque `GET /accounts` é read-only — resolveInvoice cria row se não existir (efeito colateral impróprio pra GET). O list só lê o que já existe; se o ciclo não tiver invoice materializada, `currentInvoice=null` (materialização acontece na primeira transação do ciclo via listener existente). Testes novos no repo spec: checking → null/0; credit sem invoice open → null/0; credit com invoice open → embed correto + usagePct calculado; usagePct cap em 100 quando totalAmount > creditLimit; rounding correto (100/300 → 33). Tool `list_my_accounts` do assistente ganha os campos automaticamente; playbook atualizado explicando `currentInvoice` + `usagePct` e sugerindo uso pra "quanto tô devendo no cartão X" / "quanto ocupei do limite".
- [x] **MNT-218** [T][S] ✅ commits `5034563` → `c39ba45` → `316ca4a` → `53c825e`. `GET /dashboard/view` — endpoint dedicado que agrega tudo que o dashboard `/` precisa numa resposta só. Novo módulo `api/src/dashboard/` (leve, sem entities próprias — orquestra services de accounts + transactions + card-billing). Shape resposta:
  - [x] **Bundle 1: skeleton + summary** ✅ commit `5034563`. Novo módulo `api/src/dashboard/` (Clean Arch: `application/use-cases/`, `domain/types/`, controller, module). `GetDashboardViewUseCase` orquestra `ListMyAccountsUseCase` + `ListTransactionsUseCase` (filtrado pelo mês corrente via `Clock` port, bounds `Date.UTC(y, m, 1)` e `Date.UTC(y, m+1, 1)` exclusivo). Endpoint `GET /dashboard/view` retorna `{ summary: { totalBalance, monthIncome, monthExpense, monthNet } }`. Testes: 4 do use-case (shape, month scoping via Clock, month rollover Dec→Jan, `Promise.all` paralelismo) + 2 do controller (401 sem token, 200 com payload + userId do JWT). Suite ampla: 763 passed / 15 falhas pré-existentes em `agent/` (baseline documentado desde MNT-158) — zero regressão. Lint zerado. **Pendência**: use-cases `List*` trazem `items` que o dashboard descarta — refactor pra `SummarizeCheckings` + `SummarizeTransactions` use-cases dedicados quando latência bater
  - [x] **Bundle 2: topCategories** ✅ commit `c39ba45`. Port `CategoriesRepository` ganha `getTopSpentInMonth({ userId, dateFrom, dateTo, limit })` retornando `TopSpentCategory[]` (já com `share`). Impl no `PrismaCategoriesRepository` num `$queryRaw` único: CTE `month_total` (COALESCE SUM de todos os expenses no período) + INNER JOIN `transactions t` × `categories c` + GROUP BY `c.id` + ORDER BY spent DESC + LIMIT — `SUM(t.amount)::float8` retorna number nativo (sem `decimalToNumber`), `share` via `CASE WHEN total>0 THEN sum/total ELSE 0 END` inline. Zero `.map`/`.filter`/`Map`/`push` no TS — retorno do `$queryRaw` vira o array final. Novo use-case exportado `GetTopSpentCategoriesUseCase` (delegate 1:1 pro port). `GetDashboardViewUseCase` estende `Promise.all` pra 3 fontes e retorna `topCategories` direto (sem reshape). Type `DashboardTopCategory` removido (espelho de `TopSpentCategory`); `DashboardView.topCategories: TopSpentCategory[]` reusa o port. Testes: 1 do use-case (delegate), 3 do repo (rows raw, params parametrizados via tagged template + SQL contém CTE/JOIN/share/ORDER/LIMIT, empty) + 4 novos no use-case do dashboard (topCategories retornado direto, month scoping + limit=5, 3-way parallel). Suite: 769 passed / 15 baseline agent. Lint zerado. **Decisão de arquitetura**: query cross-domain (categories × transactions) vive em `CategoriesRepository` porque retorna categorias — mesmo padrão de `TransactionsRepository.summarize` e `UserBankAccountsRepository.summarizeCheckings`. Manter em `dashboard` teria criado repo meta com SQL cross-schema, quebra Clean Arch
  - [x] **Bundle 3: monthlyFlow** ✅ commit `316ca4a`. Port `TransactionsRepository` ganha `getMonthlyFlow({ userId, now, monthsBack })` retornando `MonthlyFlowRow[]` (`{ monthKey, income, expense }`). Impl no `PrismaTransactionsRepository` num `$queryRaw` único: CTE `month_series` via `generate_series(0, monthsBack-1)` + `date_trunc('month', now - interval '1 month' * gs)` gera N slots + CTE `month_sums` com `SUM(CASE WHEN type='income' THEN amount ELSE 0 END)::float8` (idem expense) + `LEFT JOIN` do series com sums → zero-padding automático de meses sem transação. `to_char(..., 'YYYY-MM')` gera `monthKey`, aspas duplas no alias `AS "monthKey"` preservam camelCase. `ORDER BY month_start ASC`. Zero JS reshape. Novo use-case exportado `GetMonthlyFlowUseCase` (delegate 1:1). `GetDashboardViewUseCase` estende `Promise.all` pra 4 fontes; `monthlyFlow` retornado direto. Passa `now` do `Clock` como parâmetro → SQL testável, sem `NOW()` do Postgres. Testes: 1 do use-case (delegate), 3 do repo (rows raw, params + SQL contém generate_series/date_trunc/LEFT JOIN/CASE WHEN, empty) + 3 novos no use-case do dashboard (monthlyFlow no shape, scoping = 6 meses, 4-way parallel). Suite: 775 passed / 15 baseline agent. Lint zerado. **Divergência da spec**: shape não inclui `monthLabel` — formatação locale-aware é apresentação (cai no MNT-216 de i18n); cliente localiza via `Intl.DateTimeFormat` sobre `monthKey`
  - [x] **Bundle 4: balanceChart** ✅ commit `53c825e`. Port `UserBankAccountsRepository` ganha `getBalanceChart({ userId, now, days })` retornando `BalanceChartPoint[]` (`{ date, balance }`). Impl no `PrismaUserBankAccountsRepository` num `$queryRaw` único com **walking backwards via window function**: CTE `day_series` via `generate_series(0, days-1)` gera N slots + CTE `day_deltas` com `SUM(CASE WHEN type='income' THEN amount ELSE -amount END)::float8` filtrado por `a.credit_limit IS NULL` (só checking) + CTE `current_balance` (soma dos balances atuais das checking) + CTE `combined` (`LEFT JOIN` day_series × day_deltas — zero-padding). Query final: `cb.total - SUM(delta) OVER (ORDER BY day DESC ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING)` — pra cada dia, subtrai a soma dos deltas dos dias posteriores. `to_char(day, 'YYYY-MM-DD') AS "date"` preserva camelCase. `ORDER BY day ASC` (antigo → recente). Zero JS reshape. Novo use-case exportado `GetBalanceChartUseCase` (delegate 1:1). `GetDashboardViewUseCase` estende `Promise.all` pra 5 fontes; `balanceChart` retornado direto. Passa `now` do `Clock` como parâmetro. Testes: 1 do use-case (delegate), 3 do repo (rows raw, params + SQL contém generate_series/credit_limit IS NULL/CASE WHEN/OVER ORDER BY, empty) + 3 novos no use-case do dashboard (balanceChart no shape, scoping days=30, 5-way parallel). Suite: 781 passed / 15 baseline agent. Lint zerado no escopo. **Decisão de arquitetura**: `getBalanceChart` vive em `UserBankAccountsRepository` — balance é sujeito de accounts; coupling accounts↔transactions já existia via balance auto-update (MNT-130). **Divergência da spec**: implementação inteira em SQL (walking backwards via window function no Postgres) em vez de "iterar em JS pelas transactions em ordem reversa" — mesmo output, zero reshape
  - `summary: { totalBalance, monthIncome, monthExpense, monthNet }` — `totalBalance` reaproveita accounts summary (MNT-158); os três de mês derivam do `TransactionsRepository.list` filtrado pelo mês corrente (server-side, timezone default até MNT-216 landar preferred_locale)
  - `topCategories: Array<{ id, name, icon, color, spent, share }>` — top 5 por spent no mês corrente. Prisma `groupBy` em `transactions` por `categoryId` com `_sum: { amount }` where `type=expense` + join com `categories` no mesmo query. `share = spent / totalMonthExpense` (0-1)
  - `monthlyFlow: Array<{ monthKey, monthLabel, income, expense }>` — últimos 6 meses. Um único query com `date_trunc('month', occurred_at)` agrupando + sum por type (Postgres raw ou Prisma `groupBy` com múltiplos truncs). Backend devolve valores **absolutos** (income/expense em BRL), NÃO percentagens — client normaliza pra render de barra
  - `balanceChart: Array<{ date, balance }>` — 30 pontos (últimos 30 dias). Deriva walking backwards: pega `account.balance` atual, itera pelas transactions do período em ordem reversa, acumula deltas via `signedAmount` e emite um ponto por dia. Considera **todas** as checking accounts do user somadas (não credit). Backend devolve dados brutos; client faz o SVG path (formatação pura, escape hatch da regra "no reshape client")
  - Use-case `GetDashboardViewUseCase` orquestra as 4 fontes; injeta `TransactionsRepository`, `UserBankAccountsRepository`, `CategoriesRepository`
  - Tool opcional `get_dashboard_view` pro agent — playbook orienta usar quando user pede "resumo geral"/"panorama financeiro"
  - Testes: use-case com stub dos 3 repos (KPI aggregation correta; topCategories ordenados+share soma 1; monthlyFlow 6 meses zero-padded quando sem data; balanceChart 30 pontos válidos). Controller e2e cobrindo shape completa
  - **Alternativa considerada e descartada**: derivar balance histórico via snapshot table (`daily_balance_rollup`) — mais eficiente em escala mas exige migration + job/listener pra popular. Rolling backwards funciona pra escala atual (< 10k transactions/user); refactor pra snapshot quando latência bater. Anotar como pendência quando MNT-218 for revisado

---

## Fase 2 — Categorias

- [x] **MNT-127** [T][S] `CategoryRepository` + use-cases `ListCategories(userId)` (retorna defaults globais + custom do user), `AddCategory({ name, icon?, color? })`, `RenameCategory`, `DeleteCategory` (só custom; defaults globais não deletáveis). Endpoints REST ✅ commit `b18ba3e` (defaults globais protegidos pelo WHERE userId=X — não match, retorna null; RenameCategory limitado a name — icon/color por tool separada depois se necessário)
- [x] **MNT-128** [T][S] Tools (bundle): `list_categories`, `add_category`, `rename_category`, `delete_category`. Playbooks curtas — categoria é conceito simples ✅ commit `b18ba3e` (playbooks orientam a checar existentes antes de criar; global vs custom explicado sem cross-reference)
- [x] **MNT-160** [T][S] `monthlyBudget` opcional por categoria — Prisma schema: adicionada coluna `monthly_budget Decimal(15,2) NULL` no `Category` (CHECK `> 0` fica pra migration SQL manual quando `prisma migrate dev` rodar — schema.prisma não expõe CHECK constraints; Zod DTO já enforça `positive()` na app). Port `Category` ganha `monthlyBudget: number | null`; `AddCategoryInput` ganha `monthlyBudget?`; novo type `UpdateCategoryInput` (id + userId obrigatórios, resto opcional) substitui `RenameCategoryInput`. Prisma repo: `CATEGORY_SELECT` inclui `monthlyBudget`, novo `toDomain` mapper faz Decimal→number|null, método `rename` → `update` aceitando patches parciais. Use-case `RenameCategoryUseCase` → `UpdateCategoryUseCase` (arquivo renomeado via `git mv`). DTO `renameCategorySchema` → `updateCategorySchema` (todos os campos opcionais + `.refine` exigindo ao menos 1). Controller `PATCH /categories/:id` passa a aceitar shape ampla. Tool `rename_category` → `update_category` — playbook novo explicando `monthlyBudget` (BRL, null explícito limpa, um por categoria, não cumulativo). Testes: 4 casos no `update-category.use-case.spec` (multi-field, partial name-only, null clears budget, not-found); 4 casos no `update-category.tool.spec` (happy, validation reject empty, null clears, not-found error); controller spec atualizado com fixture de monthlyBudget e novo caso 400 pra body vazio; **novo `prisma-categories.repository.spec`** (5 casos — mapper preserva null, addCustom com budget, update partial, update null clear, update sem match retorna null). Suite api: baseline 15/734 pré-MNT-160 → 15/739 (+5 verdes, zero regressão).
- [x] **MNT-161** [T][S] Enriquecer `GET /categories` com `spent: number`, `usagePct: number`, `overBudget: boolean` do mês corrente. Novo type `CategoryWithUsage extends Category` no port; `CategoriesRepository.listForUser` retorna o shape enriched (add/update seguem retornando `Category` base). Repo faz **duas queries em paralelo** (via `Promise.all`): `category.findMany` (rows) + `transaction.groupBy({ by: ['categoryId'], where: { userId, type: 'expense', occurredAt: { gte: monthStart, lt: monthEnd }, categoryId: { not: null } }, _sum: { amount: true } })` (sums por categoryId). Merge em memória via `Map<categoryId, spent>`. Mês corrente = `now` server-side: `monthStart = new Date(year, month, 1)`, `monthEnd = new Date(year, month + 1, 1)` (bound exclusivo). Compute inline no `.map`: `spent = spentByCategory.get(row.id) ?? 0`, `usagePct = computeUsagePct(spent, monthlyBudget)`, `overBudget = monthlyBudget !== null && spent > monthlyBudget`. **Nota Prisma**: `_sum.amount` retorna `Prisma.Decimal | null` no runtime (a `$extends` result do MNT-162 só aplica em model rows, NÃO em agregados/groupBy) — desembalado via helper compartilhado `decimalToNumber` (ver MNT-219). **Refactor de-duplicação**: `computeUsagePct` (idêntica em `prisma-user-bank-accounts.repository.ts` desde MNT-159, ia ser copiada aqui) extraída pra util compartilhada `api/src/finance/@shared/utils/compute-usage-pct.ts` (assinatura genérica `(used, cap)`: 0 quando cap null/<=0, capped em 1, rounded). Accounts repo passa a chamar `computeUsagePct(currentInvoice?.totalAmount ?? 0, creditLimit)` — semanticamente idêntico. Novo `compute-usage-pct.spec` cobre 6 casos (null cap, cap<=0, rounding, cap at 100, exact 100 when equal, zero used). Tool `list_categories` do assistente ganha o shape enriquecido automaticamente via use-case; playbook atualizado explicando `spent`/`monthlyBudget`/`usagePct`/`overBudget` e sugerindo uso pra "quanto gastei em X esse mês" / alertas de estouro. Testes novos no `prisma-categories.repository.spec`: (a) multi-categoria com spent+usagePct+overBudget corretos, (b) categoria sem expenses → tudo zero/false, (c) groupBy é chamado com filtros certos (user + expense + bounds do mês), (d) usagePct rounded (100/300 → 33).
- [x] **MNT-219** [T][S] ✅ commit `3907917` — Mover `summary` de `/accounts` e `/transactions` do JS reduce pra Postgres aggregate — precisão exata e sem drift de float em somas de Decimal. **Accounts**: `UserBankAccountsRepository.summarizeCheckings(userId)` novo método usa `prisma.userBankAccount.aggregate({ where: { userId, creditLimit: null }, _sum: { balance, overdraftLimit }, _count: { _all } })`; `ListMyAccountsUseCase` roda `Promise.all([listByUserId, summarizeCheckings])` (paralelo, uma trip ao DB por query mas concorrentes). **Transactions**: `TransactionsRepository.summarize(filters)` novo método usa `prisma.transaction.groupBy({ by: ['type'], where: <mesmos filtros do list>, _sum: { amount: true } })`; `net = totalIncome - totalExpense` (subtração escalar sobre dois valores já somados no banco, não iteração). Extracted helper `buildWhere(filters)` no repo pra deduplicar cláusula entre `list` e `summarize`. `ListTransactionsUseCase` também vai pra `Promise.all([list, summarize])`. **Helper compartilhado** `api/src/finance/@shared/utils/decimal-to-number.ts` — a `$extends` do MNT-162 só aplica em model rows (findMany/findFirst/…); em `aggregate`/`groupBy` o Prisma devolve `Prisma.Decimal` cru no runtime mesmo que o tipo TS diga `number`. O helper faz o unbox `(Decimal | number | null | undefined) → number` via `typeof` guard. Usado nos 3 sites de agregado (accounts summary, transactions summary, categories spent). Specs novos: `summarizeCheckings` (2 casos — totais paginados, zero quando sem checkings), `summarize` de transactions (4 casos — totais por tipo, filtros forwarded pra where, zero quando sem rows, missing group tratado como zero); use-case specs ganham cobertura de `Promise.all` (ordem paralela verificada via `mockImplementation`).

---

## Fase 3 — Transações

- [x] **MNT-129** [T][S] `TransactionRepository` + use-cases `AddTransaction`, `EditTransaction`, `DeleteTransaction`, `ListTransactions(filters)`. Filtros: dateRange, accountIds, categoryIds, types, textSearch, limit/offset. Endpoints REST ✅ commit `9ef6a99` (list aceita filtros de array via query multi-value estilo `?types=expense&types=income`; `AccountNotFoundError` reutilizado do módulo accounts pra falha de ownership em add/edit)
- [x] **MNT-130** [T][S] Balance auto-update — todo insert/edit/delete de transaction ajusta `user_bank_accounts.balance` **na mesma transação DB** (BEGIN/COMMIT), pra nunca ficar dessincronizado. Testes cobrindo: rollback em erro no meio (nem transaction nem balance mudam), edit muda valor (delta aplicado ao balance), delete reverte ✅ commit `9ef6a99` (matemática do delta em `signed-amount.ts` + testes do PrismaTransactionsRepository mockando `$transaction` cobrem edit-delta/delete-reverse/account-not-owned; rollback real em erro fica pra bundle Testcontainers)
- [x] **MNT-131** [T][S] Tools (bundle) — cada com playbook detalhada: ✅ commit `a50dae5` (6 tools registradas em `transactions/tools/`; alem do CRUD single, tem **add_transactions** e **edit_transactions** batch — ambos rodam N operações num único `$transaction` do Prisma, atomicamente; max 50 itens por batch)
  - `add_transaction({ accountId, type, amount, description, categoryId?, occurredAt })` — **playbook crítico**: amount sempre positivo (type diferencia expense/income); confirmar todos os 4 campos essenciais antes; nunca inventar data; sugerir categoria mas confirmar; se cartão, informar o user que a transação entra na próxima fatura
  - `add_transactions({ transactions: [...] })` — batch atômico (bonus além da spec): user reporta múltiplos gastos numa mesma frase, agente cria todos de uma vez ou nenhum
  - `edit_transaction({ id, ...campos })` — playbook: user pode dizer "aquele ifood de ontem era 55 não 45", assistente busca por description+date antes; sempre confirma antes de editar
  - `edit_transactions({ edits: [...] })` — batch atômico (bonus): correções em lote (reclassificar N gastos de categoria X pra Y)
  - `delete_transaction({ id })`
  - `list_transactions({ filters })` — playbook: nunca retorna tudo; sempre com dateRange (default: mês corrente) e limit (default: 50)

---

## Fase 4 — Transferências

- [x] **MNT-132** [T][S] `TransferRepository` + use-cases `CreateTransfer({ fromAccountId, toAccountId, amount, description?, occurredAt })`, `ListTransfers(filters)`, `DeleteTransfer({ id })`. Validações: `fromAccountId !== toAccountId`, ambas as contas pertencem ao user, `amount > 0` ✅ commit `c00264f` (from=to rejeitado no use-case via SameAccountTransferError → 400; ownership via updateMany.count no repository → AccountNotFoundError → 404; amount>0 via zod DTO → 400; list aceita accountIds array e filtra transfers em que a conta aparece como from OU to)
- [x] **MNT-133** [T][S] Ajuste atômico de saldo em ambas contas na mesma tx DB. Delete reverte. Testes: from=to rejeitado; contas de outro user rejeitadas; saldo consistente após operações mistas ✅ commit `c00264f` (create debita from e credita to no mesmo `$transaction` do Prisma; delete inverte a operação; testes mockam `$transaction` cobrindo os 3 caminhos + rejeição por ownership em qualquer das duas contas; rollback real em erro mid-tx fica pra bundle Testcontainers)
- [x] **MNT-134** [T][S] Tools: `create_transfer`, `list_transfers`, `delete_transfer`. Playbook do create enfatiza: confirmar 3 campos (from, to, amount); avisar que não conta como gasto no dashboard ✅ commit `c00264f` (3 tools registradas via @RegisterAssistantTool em `transfers/tools/`; sub-módulo `transfers-tools.module.ts` dedicado no padrão dos outros domínios; validação via zod dentro do execute; erros de domínio (SameAccountTransferError, AccountNotFoundError, TransferNotFoundError) viram ok:false)

---

## Fase 5 — Faturas de cartão de crédito

- [x] **MNT-135** [T][S] Entity `credit_card_invoices` já criada em MNT-122. Adicionar `InvoiceRepository` + status enum (`open` / `closed` / `paid` / `overdue`). Regra: cartão só tem 1 invoice `open` por vez; N `closed`, N `paid`, N `overdue` ✅ commit `dc87aa4` (novo sub-módulo `src/finance/card-billing/` sob a estrutura consolidada — nome evita a confusão de "credit-cards" soar como armazenamento de cartão; port `InvoicesRepository` com `create` + `findOpenForAccount`; enum `InvoiceStatus` espelha o Prisma; regra "1 open por conta" enforçada no repo Prisma dentro de `$transaction` — findFirst + create atômico; `MultipleOpenInvoicesError` sinaliza violação; resto dos métodos do port (findByAccountAndCycle, list, close, markPaid, etc) fica pra MNT-136..140 conforme cada task precisar)
- [x] **MNT-136** [T][S] `CreditCardCycleService` — `resolveInvoiceForDate(accountId, date): Invoice`:
  - Dado data e conta cartão, calcula qual ciclo essa data cai (baseado em `close_day`)
  - Se invoice desse ciclo já existe, retorna. Se não, cria com `status='open'` e boundaries corretas
  - `cycle_start` = dia seguinte ao último close; `cycle_end` = próximo close; `due_date` = próximo `due_day` após close
  - Testes edge case: mês com 30 vs 31 dias, fevereiro, close_day > último dia do mês (usa último dia como fallback) ✅ commit `74e42b9` (matemática pura em `domain/utils/cycle-math.ts` usando **dayjs + plugin utc** — decisão de biblioteca registrada em `docs/adr/0001-dayjs-para-manipulacao-de-datas.md`; service em `domain/services/credit-card-cycle.service.ts`; port ganhou `findByAccountAndCycle`; assinatura do service recebe `{ accountId, date, closeDay, dueDay }` — NÃO busca account internamente pra desacoplar de `accounts/` módulo, o caller MNT-137 vai passar já com esses dados; 12 tests do util cobrem: date-em-close-day/1-day-após, dueDay<=closeDay rola pro mês seguinte, closeDay=31 em fev/abr/leap-year, virada de ano, fallback de dueDay > último dia do mês; 3 tests do service cobrem hit/create + closeDay=31 em fev)
- [x] **MNT-137** [T][S] Integração com transactions — quando `AddTransaction` recebe conta que tem `credit_limit`, chama `CreditCardCycleService.resolveInvoiceForDate(accountId, occurredAt)` e amarra `transaction.invoice_id`. Delete/edit atualiza `invoice.total_amount` da(s) invoice(s) afetada(s)
  - **[a] Add + AddMany** ✅ commit `2f7ab77` (accounts ganha `findById` + `GetAccountByIdUseCase` exportado; `AddTransactionInput` port ganha `invoiceId?`; `AddTransactionUseCase` e `AddManyTransactionsUseCase` orquestram: buscam account, se `creditLimit !== null` resolvem invoice via `CreditCardCycleService`, passam invoiceId pro repo; repo (dentro do mesmo `$transaction`) seta `invoice_id` no insert e incrementa `invoice.total_amount` por `-signedAmount` — matemática oposta do balance porque invoice rastreia "quanto o user deve", expense aumenta / income-refund reduz; batch usa cache por accountId pra economizar findById em transactions da mesma conta; `TransactionsModule` importa `AccountsModule` + `CardBillingModule`)
  - **[b] Delete integration** ✅ commit `e50cb82` (repo lê `invoice_id` no findFirst + reverte `invoice.total_amount` por `+signedAmount` — matemática oposta do Add, resultado líquido zero; se `invoice_id` for null (débito), pula update. Batch delete implícito via cascata FK — não há tool `delete_transactions` batch atualmente)
  - **[c] Edit integration** ✅ commit `d7f60ac` (port ganha `TransactionsRepository.findById` + `EditTransactionInput.newInvoiceId?: string | null`; `EditTransactionUseCase` busca current → determina newAccountId/newOccurredAt (defaults do current quando input omite) → busca account → resolve invoice se cartão → passa `newInvoiceId` (string ou null) pro repo; batch `EditManyTransactionsUseCase` faz o mesmo com cache por accountId; repo `editWithinTx` lida com os 5 casos (debit→debit, card→same-invoice, card→diff-invoice, card→debit, debit→card) via matemática oposta do balance — `oldEffect - newEffect` na mesma invoice, `+oldEffect` no revert, `-newEffect` no apply; `transaction.update.data.invoiceId` recebe `newInvoiceId` pra atualizar a FK)
- [ ] **MNT-138** [DEFERRED][T][S] Worker/scheduler diário (job simples com `@nestjs/schedule` `Cron('0 3 * * *')`):
  - Fecha invoices cujo `cycle_end` foi ontem — marca `status='closed'`, congela `total_amount`
  - Marca `overdue` invoices `closed` cujo `due_date` passou sem `paid_at`
  - Idempotente (roda 2x no mesmo dia sem duplicar efeito)
  - **Adiado por decisão do humano (2026-07-20):** infra base (`InvoicesRepository` + `CreditCardCycleService`) já cobre find/create manual. Automatização de close/overdue fica pra quando houver deploy running com cron ativo
- [x] **MNT-139** [T][S] Detecção automática de pagamento de fatura: quando `CreateTransfer` é chamado com `to_account_id` sendo cartão E `amount` matches valor de invoice `closed` não paga, marca essa invoice `paid` + salva `paid_via_transfer_id`. Se o valor não match exato, transfer é criado normal mas invoice fica `closed` — user precisa pagar/informar manualmente via tool ✅ commit `4c735e7` (evento `finance.transfer.created` em `transfers/domain/events/` emitido pelo `CreateTransferUseCase` após create bem-sucedido; listener `TransferCreatedListener` em `card-billing/infrastructure/events/` escuta via `@OnEvent` — desacoplamento por evento evita circular dep entre `TransfersModule` e `CardBillingModule` (que já se importam via `PayInvoiceUseCase`); listener verifica `toAccountId` cartão via `GetAccountByIdUseCase`, lista invoices `closed` da conta e faz match exato de `totalAmount === amount`; se match, `markPaid(inv.id, occurredAt, transferId)`; silent em qualquer não-match — user resolve manual via `mark_invoice_paid` fallback quando valor difere)
- [x] **MNT-140** [T][S] Tools (bundle): `get_current_invoice({ accountId })`, `list_invoices({ accountId, status? })`, `pay_invoice({ invoiceId, fromAccountId })` (cria transfer + marca paid), `mark_invoice_paid({ invoiceId })` (fallback manual). Playbook de `get_current_invoice` enfatiza que assistente use isso pra responder "quanto tô devendo no cartão?" ou "quando fecha minha fatura?"
  - **[a] Read tools** ✅ commit `5a3ac1a` (2 tools + 2 use-cases: `GetCurrentInvoiceUseCase` chama `findOpenForAccount` com ownership via `GetAccountByIdUseCase`; `ListInvoicesUseCase` chama novo `listByAccount(accountId, status?)` no port com ownership; `CardBillingModule` passa a importar `AccountsModule`; sub-módulo `CardBillingToolsModule` novo em `src/agent/tools/card-billing/` agregado no `ToolsModule` central; playbook do list evita cross-ref pra outras tools pra passar no linter)
  - **[b] Write tools** ✅ commit `10c7100` (2 tools + 2 use-cases + 2 novos erros de domínio (`InvoiceNotFoundError`, `InvoiceAlreadyPaidError`); port ganha `findByIdForUser(id, userId)` — join com `account.userId` pra ownership — e `markPaid(id, paidAt, paidViaTransferId?)`; `PayInvoiceUseCase` orquestra `CreateTransferUseCase` do módulo transfers pra criar movimento com valor=`invoice.totalAmount` from→cartão, depois `markPaid` com `paidViaTransferId`; `MarkInvoicePaidUseCase` só marca sem transfer (fallback manual quando user pagou por fora); ambos usam `Clock` port pro timestamp e rejeitam invoice já paga; `CardBillingModule` passa a importar `TransfersModule`; playbooks reescritos sem cross-ref pra passar no linter)

---

## Fase 5.5 — Parcelamento (compras parceladas no cartão)

Essencial pra V1 — cartão é o meio principal e parcelamento é onipresente no BR. Sem isso, "quanto vou pagar mês que vem?" mente porque ignora parcelas futuras.

**Decisões dessa fase:**
- **Sem juros** por default (parcelas iguais, `total = installments * amount`). Diferenças de arredondamento de centavo vão pra última parcela
- **Cartão só**: `add_installment_purchase` só aceita conta com `credit_limit` (é sinal de que é cartão). Compra parcelada em débito → registra como transação normal
- **Reconhecimento no assistente**: playbook detalhada pra parsing de expressões PT-BR ("Nx de R$", "N vezes de R$", "parcelei em N", "N parcelas de R$", "R$X em N vezes"). Sempre confirma valor total + N parcelas antes de invocar
- **Cancelamento em lote**: 1 tool deleta o grupo inteiro (todas as parcelas + ajusta invoices/saldos)

- [x] **MNT-154** [T][S] Migration: entity `installment_groups` (`id`, `user_id FK`, `account_id FK`, `total_amount`, `installments_count INT`, `installment_amount`, `description`, `category_id nullable FK`, `purchase_date TIMESTAMPTZ`, `created_at`, `updated_at`). `transactions` ganha colunas nullable `installment_group_id UUID FK` + `installment_number INT` (1..N). Check constraint: se um NOT NULL, ambos NOT NULL. Check adicional: `installments_count >= 2` (parcelamento em 1x = transação normal) ✅ commit `c54294d` (schema-only, mesmo padrão de MNT-122; `InstallmentGroup` model adicionado com FKs pra User cascade / UserBankAccount restrict / Category setnull; back-relations em User/UserBankAccount/Category; `Transaction` ganha `installmentGroupId?` + `installmentNumber?` + relation FK Restrict — Restrict protege o grupo de ser deletado enquanto tem parcelas ativas, forçando o cancel-flow a deletar transactions primeiro; índice em `installmentGroupId`; CHECK constraints (installmentsCount>=2, ambos NOT NULL juntos) ficam pra migration SQL futura — Prisma 7 ainda não expõe @@check GA)
- [x] **MNT-155** [T][S] Tool `add_installment_purchase({ accountId, totalAmount?, installmentAmount?, installmentsCount, description, categoryId?, occurredAt })` — cria `installment_groups` row + materializa N `transactions` (cada uma com `occurred_at = addMonths(occurredAt, i-1)`, `installment_number=i`, `installment_group_id=group.id`, `invoice_id` resolvido via `CreditCardCycleService` — MNT-136). Descrição de cada transaction fica `"{description} ({i}/{N})"`. **Playbook crítico**: reconhecer expressões PT-BR de parcelamento; confirmar totalAmount + count antes; se juros mencionados, avisar que V1 registra sem juros (soma linear) e perguntar se ok — se não, orientar user a passar valores exatos por parcela via múltiplos `add_transaction`; se conta não tem `credit_limit`, erro claro sugerindo `add_transaction` normal ✅ commit `08ded9e` (sub-módulo novo `finance/card-billing/installments/`; `InstallmentGroupsRepository` port com `createGroupWithInstallments` atômico (group + N transactions + balance + invoice.total_amount tudo num `$transaction`); `AddInstallmentPurchaseUseCase` orquestra: valida `installmentsCount>=2`, valida account é cartão, aceita `totalAmount` OU `installmentAmount`, distribui residual de centavo pra última parcela via `floor` das anteriores, pré-resolve invoice de cada parcela via `CreditCardCycleService` (dayjs `addMonths(i-1)`); tool `add_installment_purchase` com playbook orientando parsing PT-BR e sem cross-ref pra outras tools; 3 erros novos `InvalidInstallmentsCountError`, `InvalidInstallmentAmountsError`, `InstallmentPurchaseNotAllowedError`; `InstallmentsModule` importa `CardBillingModule` + `AccountsModule` e é registrado no `FinanceModule` aggregator; `CardBillingToolsModule` provê o novo tool)
- [x] **MNT-156** [T][S] Tool `cancel_installment_purchase({ groupId })` — ownership check, deleta em transação DB atômica: (a) as N transactions do group, (b) ajusta `balance` da conta, (c) ajusta `total_amount` das invoices afetadas, (d) deleta o group. Retorna resumo `{ deletedCount, affectedInvoices, refundedAmount }` pro assistente confirmar ✅ commit `44adc74` (port ganha `cancelGroup(groupId, userId): { deletedCount, affectedInvoiceIds[], refundedAmount }`; adapter Prisma faz tudo num `$transaction`: findFirst por ownership → `InstallmentGroupNotFoundError` se null → findMany das N transactions do group → loop reverte balance por `-signedAmount` + reverte invoice.total_amount por `+signedAmount` (invoice pode ser null pra parcela, skip); `deleteMany` transactions ANTES de `delete` group porque FK Restrict; matemática de reversão bate com MNT-137 [b]; `CancelInstallmentPurchaseUseCase` delega direto pro repo; tool `cancel_installment_purchase` com playbook enfatiza IRREVERSÍVEL + confirmar antes; erro novo `InstallmentGroupNotFoundError`)
- [ ] **MNT-157** [T][S] `AvailableLimitService` — `calculate(accountId): { totalLimit, currentInvoiceAmount, closedUnpaidAmount, futureCommittedAmount, usedTotal, available }` só pra contas com `credit_limit`. `futureCommittedAmount` = soma das transactions em invoices ainda não existentes ou em invoices `open` com `occurred_at > hoje` (= parcelas futuras). Endpoint `GET /banks/:id/limit-info`. Tool `get_available_limit({ accountId })` pro assistente responder "quanto tenho de limite?". Card do cartão em `/banks` mostra decomposição visual (fatura atual + faturas fechadas + comprometido futuro)

---

## Fase 6 — UI

Tarefas de UI (MNT-141, 142, 143, 144, 145) migraram pra `specs/009-ui-shell/tasks.md`. Contexto de domínio (badge de fatura, layout de cartão de crédito, `<TransactionDetail>`, histórico de invoices) preservado no texto migrado.

---

## Fase 7 — Segurança e integração

- [ ] **MNT-146** [SEC] Ownership checks universais — todos os use-cases usam `AssistantContext.userId` da sessão; testes validam que payload com `accountId` de outro user falha; injection defense em `ListTransactions` (todos os filtros via Prisma Client API tipada; `$queryRaw` só com params validados por Zod — nunca string concatenation)
- [ ] **MNT-147** [T][S] Golden test integrado end-to-end — fluxo real: adicionar cartão → 5 compras → esperar close_day → confirmar invoice fechada + total certo → criar transfer da conta corrente → invoice marcada paid. Reproduz o journey completo pra garantir que todas as camadas conversam certo
- [ ] **MNT-148** [SEC] PII scrubbing na resposta do assistente — antes de mandar texto pro ElevenLabs TTS, regex remove valores estranhos: CPF/CNPJ (raro mas possível se user disser), números de cartão (nunca deve aparecer, defesa em profundidade). Log das ocorrências pra auditoria
- [ ] **MNT-241** [T][S] Backend do bank details — 2 endpoints novos pra alimentar `/banks/[id]`:
  - **(a)** `GET /accounts/:id/balance-history?monthsBack=12` — refactor do `UserBankAccountsRepository.getBalanceChart` (MNT-218) pra aceitar `accountId` específico + granularity `month`. Reusa a mesma técnica de walking backwards via `SUM(delta) OVER (ORDER BY date DESC ROWS UNBOUNDED PRECEDING AND 1 PRECEDING)`, trocando `generate_series(0, days-1)` por `generate_series(0, monthsBack-1)` e `date_trunc('day', ...)` por `date_trunc('month', ...)`. Ownership: filtra por `user_id` no JOIN. Retorna `Array<{ monthKey: string, balance: number }>` (mesmo shape do MNT-218). Ownership 404 se `accountId` não pertence ao user
  - **(b)** `GET /accounts/:id/invoices?status?` — wire REST do `ListInvoicesUseCase` (MNT-140 já existe como tool). Novo controller `InvoicesController` no módulo `card-billing`. Ownership via `findByIdForUser` (MNT-140 já implementa). Aceita `?status=open|closed|paid|overdue|scheduled` opcional. Retorna array de invoices tipado. Suporta invoices `scheduled` (MNT-226) — parcelas futuras já materializadas
  - Testes: repo spec (novo `getBalanceHistoryByAccount`), use-case spec (delegate), controller specs (auth + shape), zero regressão em `getBalanceChart` (dashboard continua verde)
  - Playbook: `list_invoices` do agent segue funcionando igual — endpoint REST é canal paralelo pro frontend, sem afetar tools

- [x] **MNT-162** [T][S] Prisma Client Extension global `Decimal → number` — `PrismaService` refatorado: agora encapsula um `PrismaClient` estendido com `Prisma.defineExtension({ name: 'decimal-to-number', result: {...} })` cobrindo todos os campos Decimal do schema (`userBankAccount.balance`/`creditLimit`/`overdraftLimit`, `transaction.amount`, `transfer.amount`, `creditCardInvoice.totalAmount`, `installmentGroup.totalAmount`/`installmentAmount`, `category.monthlyBudget`). Compute `.toNumber()` roda no query time, consumers recebem `number` direto. **Arquitetura**: PrismaService não estende PrismaClient — mantém client interno via composição e um Proxy no constructor forwarda property lookups (`this.userBankAccount`, `this.$transaction`) pro client extended quando não são métodos da classe (`onModuleInit`/`onModuleDestroy`/`client`). Declaration merging `interface PrismaService extends ExtendedPrismaClient {}` traz os tipos do client extended pra classe. **Repos afetados & limpos**: `prisma-user-bank-accounts.repository.ts` (removido `toDomain` + `.toNumber()` no `toDomainWithBank`), `prisma-transactions.repository.ts` (removido `.amount.toNumber()` do `toDomain`, do `delete` tx callback e do `editWithinTx`; types PrismaXxxGetPayload substituídos por interfaces locais hand-typed já com `number` porque `Prisma.XxxGetPayload<...>` não respeita extensions), `prisma-transfers.repository.ts` (removido `toDomain` inteiro — `findMany`/`create` retornam direto), `prisma-invoices.repository.ts` (removido `.totalAmount.toNumber()` + type PrismaInvoiceRow hand-typed), `prisma-installment-groups.repository.ts` (removido `.toNumber()` de `totalAmount`/`installmentAmount`/`installment.amount`), `prisma-categories.repository.ts` (removido `toDomain` completo — repo agora retorna direto). **Cascades pro código consumidor**: `wire-system-prompt.ts` desempacota `accountsResult.items` (era assumindo array cru), `complete-onboarding.use-case.ts` e `get-onboarding-state.use-case.ts` idem. **Tests atualizados**: helper `decimal = (n) => n` (identity, mantido pra readability dos call-sites) nos 7 repo specs — mocks retornam number direto. Fixture do `agent-realtime.gateway.spec` e `get-onboarding-state.use-case.spec` + `complete-onboarding.use-case.spec` passam a devolver `{ items, summary }` no lugar de array cru. **Métricas**: pre-MNT-162 15 fails / 734 pass → pós 15 fails / 744 pass (+10 verdes, zero regressão, mesmos 5 suites já vermelhos pré-existentes de `agent/prompts`/`agent/gateway`). Lint zero nos arquivos tocados (`wire-tts-tap` mantém 1 warning prettier pré-existente do commit `c5749878`).

---

## Fora de escopo (V1)

- Split de transação entre contas (ex: R$100 no jantar, R$50 seu R$50 amigo) — DEFERRED
- Recorrência de transação (não confundir com `recurring_rules` da Fase 6 do brief) — DEFERRED
- Anexos (foto do recibo, PDF do boleto) — DEFERRED
- Notificações antes do vencimento da fatura — DEFERRED (spec de notifications virá em algum momento)
- Fatura com mais de uma moeda (Wise, Nomad) — DEFERRED
- Parcelamento **com juros calculado automaticamente** (parcelas com valores diferentes por conta de juros) — DEFERRED. V1 cobre parcelamento sem juros (Fase 5.5)
- Estorno / crédito na fatura — DEFERRED

## Referências

- [Product Brief](../000-product-brief/spec.md) — modelo de dados financeiro
- [Auth spec](../002-auth/tasks.md) — ownership context
- [Assistant spec](../003-assistant/tasks.md) — ToolRegistry, playbooks
- [UI shell spec](../009-ui-shell/tasks.md) — onde as páginas aterrissam
- @nestjs/schedule — https://docs.nestjs.com/techniques/task-scheduling (pro job de fechamento de invoice)
