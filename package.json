{
  "name": "moneta",
  "private": true,
  "description": "Root tooling: commitlint + husky. Não é workspace pnpm — api/ e web/ têm lockfiles independentes.",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  }
}
